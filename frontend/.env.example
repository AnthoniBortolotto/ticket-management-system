# Copie para frontend/.env.local. O Next le .env.local nativamente.

# URL do backend usada pelo servidor Next (Route Handlers e Server Actions).
# O browser nunca fala com o backend direto, entao isto nao e NEXT_PUBLIC_.
API_BASE_URL=http://localhost:8080
